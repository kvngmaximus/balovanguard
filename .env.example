## Environment variables for local development and Vercel

# Client-exposed GraphQL endpoint used by the app
NEXT_PUBLIC_HYGRAPH_ENDPOINT="https://your-hygraph-endpoint-url"

# Server-only token used by /api/contact to perform GraphQL mutations
HYGRAPH_TOKEN="your-hygraph-token"

# Copy this file to .env.local and fill in real values